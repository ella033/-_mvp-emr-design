'use client';

import React from 'react';

export function EmptyDocumentContent() {
  return (
    <div className="flex items-center justify-center w-full h-full p-10 text-gray-400 border-2 border-dashed border-gray-200 rounded-lg">
      등록된 서식 컴포넌트가 없습니다.
    </div>
  );
}
